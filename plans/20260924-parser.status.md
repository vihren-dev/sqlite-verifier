# Parser foundation status

Created: 2026-09-24. Status: IN PROGRESS.

Parent task: [Step 1](20260924-step-1-schema-extensions.task.md).
Owner: conformance_review. Challenger: technical_lead.
Workspace: sqlite-verifier-parser. Base: 3ada97a8.

Outcome: recognize full default-build SQLite 3.51.0 grammar without schema
execution, emit source byte spans and generic syntax trees, reject malformed
input, and preserve valid unsupported syntax for downstream admission checks.

Implementation uses upstream Lemon to read the upstream grammar; generic tree
actions replace SQLite code-generation actions. Tokenization reuses the matching
amalgamation and contextual keyword helpers. No mini SQL grammar is authored.

Checks: bounded executable tests for script boundaries, trigger semicolons,
quoted identifiers, contextual keywords, malformed input, UTF-8 offsets, and
representative unsupported syntax. Generated artifacts remain outside commits.

Progress: read upstream source and pinned source/build/document archives;
coordinated SQLite 3.51.0 pin and proposed interface with integration engineer;
requested lead review of generator design. No parser tests run yet.

Validated implementation: all 409 pinned default-build productions generated;
20 grammar smoke scripts plus malformed SQL, UTF-8/NUL, limits, determinism and
byte-span regressions pass. Lexical numeric-separator validation was retained
from upstream sqlite3DequoteNumber after source review found it outside tokenizer.

Local build uses `CC=/usr/bin/cc python3 parser/build.py` because inherited CC
points to absent Homebrew clang; coordinator authorized explicit project compiler
selection without changing global environment. Each build subprocess has a 90s
timeout; parser subprocess tests have a 3s timeout. Pinned Nix build remains for
integration. Upstream source hashes checked on build; archive provenance recorded.

Next: independent technical-lead review and integrated Nix checks. No semantic
verification or native/model correspondence is claimed by parser recognition.
